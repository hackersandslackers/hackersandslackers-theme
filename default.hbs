<!DOCTYPE html>
<html lang="{{@site.locale}}">

  <head>
    {{!-- Basic meta - advanced meta is output with {ghost_head} below --}}
    <title>{{meta_title}}</title>
    {{!-- Theme JS --}}
    <script src="https://kit.fontawesome.com/5b4eea6520.js" crossorigin="anonymous"></script>
    <script src="{{asset 'built/main.js'}}"></script>
    <link rel="stylesheet" type="text/css" href="{{asset 'built/global.css'}}" />
    {{!-- Post meta, scripts and styles. --}}
    {{#is "post"}}
      {{> "post/head" }}
      {{else}}
        {{> "page/head" }}
    {{/is}}
    {{!-- Advanced SEO meta data --}}
    {{ghost_head}}
  </head>

  <body class="{{body_class}}">
    {{> "site-navigation" }}
    <div class="viewport">
      <div class="site-content" >
        {{!-- All other templates get inserted here, index.hbs, post.hbs, etc --}}
        {{{body}}}
      </div>
    </div>
    {{!-- The global footer at the very bottom of the screen --}}
    {{> "footer/footer" }}
    {{ghost_foot}}
  </body>

</html>